import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
    placement: {
        alignItems: "center",
        marginTop: 300,
        fontSize: 24
    },
    innerContainer: {
        alignItems: "center",
        justifyContent: "center",
        marginBottom: 10,
        width: "75%"
    },
    scrollView: {
        flex: 1,
        justifyContent: "center"
    }

})